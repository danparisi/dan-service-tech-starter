# debug: true

spring:
  application:
    name: dan-service-tech-starter
  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    org:
      springframework:
        cloud:
          openfeign: DEBUG
        retry: DEBUG